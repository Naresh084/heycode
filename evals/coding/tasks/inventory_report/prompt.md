Implement normalized inventory counting and a deterministic text report. Names should be trimmed, internal whitespace collapsed, compared case-insensitively, counted, then rendered in sorted order. Change only `inventory.py` and `report.py`.

