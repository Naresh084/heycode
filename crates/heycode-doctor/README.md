# heycode-doctor

Effect-owned asynchronous health checks with bounded, redacted schema-v1
reports. Configuration migration evidence is a closed typed vocabulary;
`UseHomeCredentialStore` reports the retirement of OS credential storage
without probing any native store or exposing credential content.

Verification: `cargo test -p heycode-doctor -p heycode-config`.
