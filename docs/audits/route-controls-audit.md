# Route and permission control comparison

The rows remain open. Root captured the installed Claude Code **2.1.269** (SHA256 `c942e1228b93cb4d52183b3dfbc77f28264f35aa947acd9c0853d029164cf450`) at 110x42 in `tmp/terminal-evidence/route-controls-claude-reference-20260911T193508Z-959b89`. This is a new version-specific observation; it does not silently relabel earlier 2.1.268 captures. HOME, configuration and workspace were disposable. Provider/proxy routes targeted closed loopback. Commands opened dialogs and were cancelled; login/logout were only typed for command-menu discovery. No model prompt, route selection, permission change, login or logout was executed.

The first helper attempt stopped at the owned workspace trust prompt; its failure is retained in `route-controls-claude-reference-20260911T193440Z-93c794`. Version two explicitly acknowledges only the created fixture and completes all eleven captures.

## Observed contracts and remaining implementation

- `/model`: an unboxed bottom list, numbered model options, current mark, capability-specific descriptive rows, inline effort and two separate commit scopes. Enter sets the default for new sessions; `s` selects only for this session; Escape cancels. Native currently has exact owner/catalog/revision checks and persistent selection, but no picker session-only action. Native multi-provider ownership must remain explicit while matching source layout.
- `/effort`: left/right selection over model-supported choices, Enter commit, `s` session-only and Escape cancel. Native exact backend vocabulary and stale-owner checks are implemented; selection scope and source-shaped presentation remain open. Do not copy unsupported Claude effort names into other providers.
- `/permissions`: Recently denied / Allow / Ask / Deny / Auto mode / Workspace tabs, search and an Add-rule entry. Native currently opens permission modes; granular rule authority and tab behavior are separate missing contracts, not a visual pass from the mode picker.
- `/sandbox`: Mode / Overrides / Config tabs, sandbox with auto-allow, sandbox with regular permissions, and no sandbox. Native must retain its actual composed platform support and authority. Source UI observation is not authorization to enable or weaken a real sandbox.
- `/login` and `/logout`: command discovery only in this capture. Their execution, cancellation, scope and provider-neutral native comparison remain unverified here.

Known source/native structural differences must be corrected before final per-row visual completion. Functional, catalog, permission and Settings tests remain useful evidence but do not replace that gate.

## Effort presentation correction in progress

The native effort picker now uses a horizontal backend-owned scale, hides the ordinary composer while open, preserves exact supported vocabulary/current/default markers, and keeps the actual owner visible. Keyboard arrows retain their existing selection semantics. A pointer probe at 110x42 against the same Claude 2.1.269 binary (`route-controls-claude-reference-pointer-20260911T195934Z-347439/02-effort-pointer-low.txt`) left its high selection unchanged when the low label was clicked. Native pointer input therefore does not change the selection; paste is consumed without altering the retained composer. This probe sent no model prompt, used disposable HOME/config/workspace and closed-loopback provider/proxy targets, and executed neither login nor logout. Its user-type journal records are local slash-command records, not evidence of model inference.

Session-only selection remains unimplemented and is not advertised in the native footer. Per-row completion is still open pending actual native rendering/keyboard checks and the remaining scope semantics. No account or model capability is inferred from Claude's displayed scale.
