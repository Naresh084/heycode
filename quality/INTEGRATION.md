# Root integration hooks

This lane does not edit the root tracker, status, gotchas, crate code or root
Cargo files. The owner must perform those integrations only after reviewing
fresh evidence.

Completed root integration:

- QSEC05's strict Tool projection now consumes the source-wrapped derived
  content. A focused Rust regression covers Web/MCP/LSP plus an exact legacy
  control.
- The rebuilt heycode product subject passes all three deny cases and the separate
  auto-approval marker control. `/tmp/qsec05-dshx-root.json` passed the
  content-free artifact validator on 2026-08-31; the temporary path is evidence
  provenance, not a checked-in artifact.

Remaining hooks:

1. Decide and record reference hardware before promoting
   `release_reference` budgets from `proposed` to `enforced`. The local/debug
   ceiling is only a regression smoke, not a release performance claim.
2. Feed the last accepted main-branch Q10 artifact to `--baseline` in the
   repository's artifact-retention system. The runner already enforces the
   declared relative threshold; this lane does not invent an unobserved
   baseline.
3. Supply two reviewed live agent config files and the required installed
   reference client before manually enabling the workflow's live job. Matching
   model/permission/sandbox/timeout declarations and config digests are
   enforced, but the operator remains responsible for making the external
   client's command honor its declaration.
4. Hosted workflow execution remains separate evidence. The local accepted
   Q10/Q11/QSEC05 runs do not claim that GitHub's scheduled job has executed.
