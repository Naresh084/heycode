{
  "name": "@heycode/sdk",
  "version": "0.1.0",
  "description": "Typed transport-neutral client for the heycode app-server protocol",
  "type": "module",
  "license": "MIT",
  "sideEffects": false,
  "engines": {
    "node": ">=24.12"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/client.d.ts",
      "import": "./dist/client.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.examples.json && node --test test/*.test.mjs && node examples/typed-client.ts"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
